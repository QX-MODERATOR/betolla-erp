# Engineering checkpoint — 2026-09-13

## Latest checkpoint — 2026-09-16, HR module phase 4 (recruitment, performance, documents) — HR plan complete
- New `supabase/migrations/025_hr_talent_documents.sql` (depends on 006, 016 for `call_logs.rep_name`, 022-024):
  - Recruitment: `hr_job_openings`, `hr_candidates` (unique phone per opening), `hr_candidate_events` timeline. `business_hr_candidate_hire` creates the employee file (probation when an end date is given), marks the candidate hired and locked, and auto-closes the opening once all positions are filled.
  - Performance: `hr_reviews` (six 1-5 criteria, overall = average, unique per employee + period, draft -> submitted -> acknowledged). HR may review anyone, a manager only direct reports, nobody themselves; submitted reviews are final. `business_hr_employee_kpis` gives orders/delivered/cancelled/sales (orders.owner_account_id), calls (call_logs.rep_name), attendance, late and leave days; the KPI snapshot is computed inside `business_hr_review_save` from a constant account->rep-name map so retries stay byte-identical.
  - Documents: `hr_documents` (metadata + expiry only, archivable, never deleted). `business_hr_expiries(days)` lists documents, contracts and probation ends; `business_hr_alerts_claim` atomically claims due reminders (30 days / 7 days / expired) in `hr_alert_log`, keyed by subject + expiry date + threshold, so each is sent once and a renewal re-arms it.
- Reminders are dispatched lazily (no cron needed): on any `/api/hr/me` load, HR dashboard/alerts load, and document saves. HR is notified for everything; employees for their own documents.
- APIs: `/api/hr/recruitment`, `/api/hr/reviews` (HR), `/api/hr/me/reviews` (own reviews + acknowledge; manager reviews and KPIs for direct reports only), `/api/hr/documents`, `/api/hr/alerts`; `/api/hr/me` now includes the caller's non-archived documents (without HR notes).
- UI: `/hr/recruitment` (openings, pipeline board, candidate detail with timeline, hire form), `/hr/performance`, `/hr/documents` (expiry filters, missing-file check, Excel), `/hr/me/reviews` (acknowledge; managers review their team), documents tab on the employee profile, documents on My Profile, sidebar entries, dashboard with server-side expiry list, workforce indicators (12-month turnover, average tenure, gender split), recruitment and review widgets.
- Verified: `scripts/test_hr.mjs` covers all 4 phases (PASS; the test adds 016's `rep_name` column itself because 016's functions need unrelated migrations). tsc PASS, focused eslint 0 errors (one `<img>` warning, same pattern as the sidebar), `next build` PASS. Live loopback sandbox: recruitment -> hire, manager review with real KPIs -> employee acknowledgement + notification, document expiry alert to HR and employee, page/API RBAC. Sandbox stopped afterwards.
- Sandbox note: PostgREST 16.3 again exited silently with code 3 right after "Starting" (several times, then fine). Migration 025 was not the cause: a manual run loaded the schema (105 RPCs) and listened. Workaround used: start PostgREST manually on 55440 with `PGRST_LOG_LEVEL=debug` in a small retry loop, PATH including the embedded-postgres `native/bin` (needs `libpq.dll`).

## Previous checkpoint — 2026-09-16, HR module phase 3 (payroll)
- New `supabase/migrations/024_hr_payroll.sql`: `hr_employees.commission_rate` (create/update RPCs redefined with `CREATE OR REPLACE` so the existing field diff audits it), `hr_settings.payroll` (SSC 7.5% / 14.25%, optional insurable-wage cap, daily basis 30, absence deduction off by default), `hr_salary_components` (recurring allowances/deductions, SSC-subject flag), `hr_payroll_adjustments` (one-off bonus/overtime/deduction/income tax per month, voidable), `hr_advances` (installments; repaid amount derived from approved/paid payslip lines), `hr_payroll_runs` (one per month, draft -> approved -> paid) and snapshot `hr_payslips` (names, bank, every amount and itemised lines frozen at calculation).
- Calculation (`business_hr_payroll_calc`): prorated by calendar days employed; commission = rate x delivered orders (`orders.owner_account_id`) in the month; unpaid leave always deducted at (basic + allowances)/daily_basis; absences (working days with no check-in and no approved leave) counted and only deducted when enabled; SSC on basic + SSC-subject allowances (capped if configured, skipped for freelancers); advance installment capped by the remaining balance; warnings NO_IBAN / NO_SALARY / NO_SSC_NUMBER / NEGATIVE_NET / FINAL_SETTLEMENT / SUSPENDED. Draft runs auto-recalculate when components, adjustments or advances change. Approve recalculates and only approves if net total and head count still match what HR reviewed (otherwise returns `changed`), and refuses negative nets. Approved/paid months lock adjustments, advances starting then, and regeneration. Reopen (HR, reason required) only before payment; pay is finance/admin/GM only and settles fully repaid advances.
- API `/api/hr/payroll` (HR full; finance read + pay; request field `kind` selects the operation, component/adjustment types are `component_kind`/`adjustment_kind`) and `/api/hr/me/payslips` (own approved/paid payslips + advances). RBAC: finance allowlist gains `/hr/payroll`. Notifications: approve -> finance, reopen -> HR, pay -> each employee with a login.
- UI: `/hr/payroll` (runs with totals/warnings, approve/reopen/pay, Excel with payroll + bank transfer + SSC sheets, printable payslips; HR tabs for monthly adjustments, advances, settings), employee profile pay tab (components editor, advances, commission), employee form commission field, `/hr/me/payslips`, dashboard payroll widget, sidebar entries. Print CSS in `globals.css` is scoped to `body.printing-area` so existing `window.print()` pages are unaffected.
- Verified: `scripts/test_hr.mjs` extended with a hand-computed payslip (PASS), tsc PASS, focused eslint 0 errors (1 `<img>` warning, same pattern as sidebar), `next build` PASS. Live loopback sandbox run: migration 024 applied, HR generate -> approve, finance pay, employee payslip + notifications, RBAC on pages. Sandbox stopped. Note: the sandbox DB had 023 applied before the leave-type ordering tweak, so its `business_hr_leave_types` is the older ordering (local only).
- Assumptions to confirm with the business: SSC wage excludes commission/bonuses; daily rate = monthly fixed pay / 30; statutory SSC wage cap left at 0 (no cap) until HR enters the current figure.

## Previous checkpoint — 2026-09-16, HR module phase 2 (attendance, leave, self-service for every account)
- New `supabase/migrations/023_hr_attendance_leave.sql`: `hr_settings` (attendance: timezone Asia/Amman, hours, grace, weekend dow list; `payroll` key reserved for phase 3), `hr_holidays`, `hr_attendance` (one row per employee/day, self punches use the DB clock, HR corrections require a reason and are audited as from/to), `hr_leave_types` (Jordan Labour Law defaults, editable, optional gender eligibility), additive `hr_leave_adjustments`, `hr_leave_requests` (working-day count excludes weekend + holidays, half days, same-year only, overlap + balance checks with pending counted as reserved). Approval: HR/admin/GM, or the employee's direct manager via self-service; nobody decides their own request; employees can cancel pending or not-yet-started approved leave. `business_hr_employee_bulk_create` onboards login accounts as employee files in one step (role -> department/title).
- Identity for self-service is always server-derived (`hrActor`: account id -> employee id); client-sent employee_id/is_hr/auto_approve are ignored on `/api/hr/me/*`.
- APIs: `/api/hr/attendance` (month sheet + corrections), `/api/hr/settings` (hours/weekend/holidays), `/api/hr/leave` (requests, decisions, types, adjustments), `/api/hr/me` (extended payload), `/api/hr/me/attendance` (GET status for header, POST punch), `/api/hr/me/leave` (own requests, manager decisions). Best-effort notifications (`lib/hr-notify.ts`) to manager + HR on request, to employee on decision.
- UI: header check-in/out button for every linked account; sidebar "خدماتي الوظيفية" (profile / my attendance / my leave) for all roles; HR pages `/hr/attendance` (monthly grid, today board, corrections, settings/holidays, Excel) and `/hr/leave` (pending, all, balances with adjustments, calendar, types); employees page gets "ملفات لحسابات النظام" bulk onboarding; dashboard shows today's attendance, pending and on-leave.
- Verified: `scripts/test_hr.mjs` extended (PASS; PGlite now returns `date` as text like PostgREST). tsc PASS, focused eslint on new files 0 problems (header.tsx has 2 pre-existing errors), `next build` PASS. Live smoke test on the local native sandbox (real Postgres + PostgREST + next dev, loopback only): migrations 022/023 applied, bulk onboarding created 17 files, self punch, self leave request + HR notification, HR endpoints, page RBAC (rep redirected from /hr/attendance, allowed /hr/me/leave). Sandbox stopped afterwards (no listeners left). Browser UI click-through NOT done (would require typing a password into the login form).
- Bulk-created files have no gender, so maternity/paternity stay hidden for those employees until HR sets gender.

## Previous checkpoint — 2026-09-16, HR module phase 1 (core records)
- Plan agreed with the user: 4 phases, one commit each, check in after each. (1) employees/departments/self-service, (2) attendance (self check-in + HR corrections with audit) and leave, (3) payroll (Jordan SSC 7.5% employee / 14.25% employer, editable rates, JOD, income tax as manual deduction; draft -> HR approved -> finance paid -> locked), (4) recruitment, performance, document metadata + expiry alerts (no file storage), full HR dashboard.
- New `supabase/migrations/022_hr_core.sql`: `hr_departments` (seeded with structural defaults mirroring lib/auth.ts roles), `hr_employees` (optional unique link to a SYSTEM_ACCOUNTS id, EMP-#### numbers, sensitive salary/bank/national-id columns), append-only `hr_audit_log` (field-level from/to diffs). `business_hr_*` RPCs in the 006-008 style (service_role only, business_requests idempotency via shared `business_hr_replay`, optimistic concurrency via `expected_updated_at`, manager-cycle guard, date CHECKs). Not applied to hosted Supabase.
- RBAC (`lib/auth.ts`): `/hr/me` + `/api/hr/me` open to every signed-in role (handler scopes to the caller's own account id, strips HR notes); `/hr`, `/api/hr` added to hr_operations; explicitly forbidden for sales_manager/sales_rep blacklists. hr_operations home is now `/hr`. Route handlers additionally require `canManageHr` (admin/GM/hr_operations).
- UI: `/hr` dashboard, `/hr/employees` directory (filters, Excel export via lazily-imported xlsx, departments modal), `/hr/employees/[id]` profile (tabs incl. audit history, edit sends only changed fields), `/hr/me` self-service; sidebar "الموارد البشرية" section.
- `scripts/test_hr.mjs` (PGlite): validation, RBAC (rep/finance/driver 403), idempotent create/replay/conflict, duplicate national id/account link, stale-update rejection, manager cycles, termination-date rule, audit diffs, self-service scoping, close/reopen durability. PASS. tsc PASS, focused eslint on new files 0 problems, `next build` PASS.
- Pre-existing, unrelated: `test_session_security`, `test_business`, `test_customers`, `test_inventory`, `test_rbac` fail identically on the untouched baseline (they sign tokens with outdated usernames such as `rahma` that verifyAuthToken now rejects). `.local-tests` PGlite had to be reinstalled (`npm install --no-save --prefix .local-tests @electric-sql/pglite pg @embedded-postgres/windows-x64@18.4.0-beta.17`, then run that package's `scripts/hydrate-symlinks.js`) — installing one package alone prunes the others.
- Not verified: live browser run of the HR pages (needs migration 022 in the local native sandbox or staging).

## Previous checkpoint — 2026-09-15 (batch C), inventory + reversible movement persistence
- Task 4. `app/api/inventory/route.ts` previously kept movements in a module-level `let movementsLog = [...]` array (reset to hardcoded fixtures on every server restart) and `app/inventory/page.tsx` used a hardcoded `INITIAL_PRODUCTS`/`INITIAL_MOVEMENTS` array (`setTimeout`-simulated writes, never persisted). Both replaced with real storage against the existing `products`/`inventory`/`inventory_movements` tables (schema already present in migration 001; migration 002 already seeds the real 31-product catalog with cost/retail/sale prices, excluded here as company data per existing convention, same as for customers/orders).
- New `supabase/migrations/008_inventory_persistence.sql`: `business_inventory_catalog`/`_movements`/`_movement_document` (reads) and `business_inventory_movement_create`/`_movement_reverse` (writes), same idempotent/advisory-locked/least-privilege style as migrations 006/007. `inventory_movements.quantity` is used exactly as the existing schema comment already specified ("positive for increase, negative for decrease") — no new columns, no schema reinterpretation.
- Correction model is **additive, never destructive**: a movement is never edited or deleted. "Reversing" a movement inserts a new movement with the exact opposite signed quantity, linked back via `reference_type='reversal'`/`reference_id=<original id>`. A reversal cannot itself be reversed, and the same movement cannot be reversed twice — both enforced in SQL, not just the UI. Stock is guarded so it can never go negative (`INSUFFICIENT_STOCK`), covering both direct movements and reversals.
- `lib/business-server.ts`: added `prepareInventoryMovement` (SKU/type/quantity validation, server-derives the signed delta — purchase_in/return_in positive, sale_out/damaged negative, adjustment takes an explicit `direction`) and `prepareInventoryReversal`. `lib/business.ts`: added `BusinessProduct`/`BusinessMovement` types.
- `app/inventory/page.tsx` rewritten to load real data (`loadBusiness`), post movements through the same idempotent retry-safe `saveBusiness` helper used by orders/finance/customers, and added a "reverse" action per movement row (disabled once a movement already has a reversal). Removed several pieces of fabricated UI copy that no longer matched reality now that real data is used (hardcoded "31 products across 6 lines", fake per-rep customer counts in a filter dropdown were already removed in the customers batch) — category pills, low-stock banner text and header counts are now all computed from the real catalog.
- New `scripts/test_inventory.mjs` (PGlite, same harness as `test_business.mjs`/`test_customers.mjs`): additive-migration legacy-stock survival, movement validation, purchase/sale stock arithmetic, negative-stock rejection, idempotent create + reversal, double-reversal blocked, reversal-of-a-reversal blocked, lost-response retry safety, full database-file close/reopen durability. PASS.
- New `scripts/test_inventory_native.mjs` (same real-native-stack harness as `test_customers_native.mjs`, run against `scripts/local_sandbox.mjs`): purchase-in through the live authenticated API -> independent SQL read -> second-session refetch (refresh-equivalent) -> sale-out -> idempotent-retry safety -> reversal restores stock -> double-reversal rejected -> logout/login retention. Then, after a genuine full cold restart of Postgres+PostgREST+Next (new OS PIDs confirmed, same procedure as the customer batch), re-ran both `test_customers_native.mjs` and `test_inventory_native.mjs` together and confirmed **both** fixtures (the customer from batch B and the product/stock from this batch) survived side by side. PASS.
- One transient failure during this restart testing: PostgREST exited immediately after printing "Starting PostgREST 16.3..." with no further log output, even from a from-scratch clean process state (confirmed zero leftover `postgres.exe`/`postgrest.exe`/sandbox `node.exe` before starting). Reproduced manually with the exact same binary/command/env on a spare port — it failed once, then succeeded on the very next attempt with identical inputs. Diagnosed as transient Windows-environment flakiness (most likely Defender/AV scanning the freshly-touched `.exe` on first launch) rather than an application, migration, or RPC defect — confirmed separately via a direct `psql`-style RPC call while it was down, which worked immediately once PostgREST was actually listening. No code change was needed; the fix was operationally retrying the sandbox launch.
- Full regression re-run after this batch: `scripts/test_inventory.mjs`, `scripts/test_customers.mjs`, `scripts/test_business.mjs`, `scripts/test_business_client.mjs`, `scripts/test_session_security.mjs` all PASS. `npx tsc --noEmit --incremental false` PASS. Focused `eslint` on every file touched this batch: 0 errors (only pre-existing-style unused-icon-import warnings, same tolerance as prior batches). `npx next build` PASS.
- Sandbox processes cleanly shut down afterward (`pg_ctl -m fast stop`, confirmed zero survivors); no background native-database processes left running.
- Explicitly out of scope for this batch (not requested, and would be a separate, larger change): automatically deducting/restoring inventory when an order's status transitions (e.g. `confirmed`→ deduct, `cancelled`/`returned` → restore). Orders (migration 006) and inventory (migration 008) remain independently persisted; wiring them together would touch `business_create_order`/`business_status` and was left alone per the instruction to keep this batch to inventory persistence itself.

## Previous checkpoint — 2026-09-15 (batch B), real native end-to-end customer/lead lifecycle
- Continuation of the same-day checkpoint below (batch A implemented persistence; this batch verifies it for real, per explicit request, instead of relying only on the PGlite unit test).
- New `scripts/test_customers_native.mjs`: runs against `scripts/local_sandbox.mjs` (real embedded PostgreSQL + real PostgREST binary + a real running `next dev` server on loopback, no production connection). Covers, in order: create a lead through the public `/api/leads` endpoint with synthetic data -> confirm the row via an independent `pg` SQL connection -> re-fetch `/api/customers` from a second, independently-obtained session (the refresh/second-tab equivalent) -> resubmit the identical phone number twice (no duplicate row either time) -> logout then fresh login, confirm still visible -> reject unauthenticated/garbage-token reads (401). It also writes a small fixture file so a later run of the same script can independently confirm the row survived in between runs.
- Verified a genuine full cold restart, not just a script re-run: force-stopped the embedded Postgres (`pg_ctl -m fast stop`), confirmed zero `postgres.exe`/`postgrest.exe`/sandbox `node.exe` processes remained, then started `scripts/local_sandbox.mjs` fresh. Confirmed via OS process IDs that Postgres came back under a **different PID**. Re-ran `scripts/test_customers_native.mjs`: PASS, including the "previous-run fixture survived" check — the customer created before the restart was still present via both direct SQL and the live API after Postgres + PostgREST + Next.js all restarted from cold.
- One transient failure occurred mid-testing (a 503 from `/api/customers` right after a restart) — root-caused to the tester's own process handling, not the app: an earlier PostgREST instance was still alive when Postgres was killed out from under it and died shortly after, while a newly-started PostgREST briefly logged nothing further; by the time this was diagnosed both had exited and a clean sequential restart (stop everything, confirm zero processes, start once) reproduced no failure. Confirmed independently via a direct `SELECT business_customer_list()` over `psql`/`pg` that the RPC and data were fine throughout — this was never a data-loss or application bug. No code change was needed to fix it.
- Full regression re-run after this verification: `scripts/test_customers.mjs`, `scripts/test_business.mjs`, `scripts/test_business_client.mjs`, `scripts/test_session_security.mjs` all PASS. `npx tsc --noEmit --incremental false` PASS. Focused `eslint` on every file touched this batch (including the two new test scripts): 0 errors, 8 pre-existing/minor warnings (one new `adminToken`-unused warning was fixed by actually exercising it in the test, rather than suppressed). `npx next build` PASS.
- Sandbox processes were cleanly shut down afterward (`pg_ctl -m fast stop`, confirmed zero survivors) so no background native-database processes are left running after this batch.
- This batch is a **checkpoint commit** (customers work only) — `android/app/build.gradle` and `lib/security.ts` remain the same pre-existing, unrelated, uncommitted edits from before this task and are deliberately excluded.

## Previous checkpoint — 2026-09-15 (batch A), customer create/read persistence
- This section supersedes older next-priority paragraphs. Implemented the exact task flagged as next on 2026-09-14: additive customer create/read persistence, connected to the CRM UI.
- New `supabase/migrations/007_customer_persistence.sql`: `business_customer_document`/`business_customer_list`/`business_customer_create` RPCs, same idempotent/least-privilege style as migration 006 (service_role only, advisory-locked, additive). Create is idempotent by phone number (a retried/duplicate lead never creates a second row) — mirrors the already-existing `supabase/functions/ingest-lead` edge function's dedup behavior rather than inventing a new one.
- `app/api/customers/route.ts` (new): authenticated GET, lists real rows. RBAC (`lib/auth.ts`) extended so `marketing_manager`/`marketing`/`hr_operations` (roles whose home page is `/customers`) can reach it — `sales_manager`/`sales_rep`/`admin`/`general_manager` already could via their existing blacklist/unrestricted rules.
- `app/api/leads/route.ts` rewritten: previously validated input and returned a fabricated success object with no database write (the exact bug flagged in the last checkpoint). Now calls `business_customer_create` via `lib/business-server.ts`'s `prepareLead`/`businessRpc`; stays unauthenticated per the documented n8n/marketing-automation contract in `n8n/README_WEBHOOKS.md`.
- `app/customers/page.tsx`: removed the hardcoded `SAMPLE_CUSTOMERS` array. Loads real rows via `loadBusiness('/api/customers')` on mount with loading/error states, and the "add lead" modal reloads the real list after a confirmed save instead of fabricating a client-side row. Also removed the hardcoded fake "45,309 records" / "page 1 of 4531" copy since real counts are now used and there is no real pagination yet.
- `lib/business.ts`: added `BusinessCustomer`/`BusinessCallLog` types (field names intentionally match the old sample data shape to keep the UI diff minimal). `lib/business-server.ts`: exported `text()`, added `prepareLead()` (phone normalization/validation, enum-checked lead source, round-robin rep fallback — same logic the old in-app/edge-function versions used).
- New `scripts/test_customers.mjs` (PGlite, same harness as `scripts/test_business.mjs`): additive-migration legacy-row survival, lead validation, phone-based idempotent create, auth-gated list (401 without a session), lost-response retry safety (no duplicate on retry after a dropped response), and full close/reopen database-file durability. PASS.
- Full regression pass this batch: `scripts/test_customers.mjs`, `scripts/test_business.mjs`, `scripts/test_business_client.mjs`, `scripts/test_session_security.mjs` all PASS (no changes to those files; run to confirm no regression). `npx tsc --noEmit --incremental false` PASS. `npx next build` PASS. Focused `eslint` on every touched file: 0 errors (7 pre-existing unused-import warnings untouched, not introduced this batch).
- NOT done in this batch: browser refresh/relogin/device verification (still only PGlite-level, not a live Next dev server + browser check), hosted Supabase/PostgREST verification, customer edit/reassignment UI, call-log recording UI (the `call_logs` table and `history` projection exist and are read by `business_customer_document`, but nothing writes to `call_logs` yet — new customers correctly show an empty history, not fabricated data). Migration 004 (driver schema) compatibility remains a separate, still-unresolved blocker for task 6 (delivery).
- Working tree still carries the pre-existing unrelated uncommitted edits to `android/app/build.gradle` and `lib/security.ts` (insecure-context Web Crypto fallback) from before this batch; left untouched and excluded from this batch's scope, per the same discipline as the 2026-09-14 checkpoint.

## Previous checkpoint — 2026-09-14, bounded native verification batch
- This section supersedes older next-priority paragraphs: password/security development remains deferred. Eight tasks remain fixed in IMPLEMENTATION_STATUS.md; no whole task is yet complete.
- Added local native PostgreSQL + official PostgREST launcher and real Next API tests. All endpoints bind loopback; Next overrides remote configuration, suppresses Telegram credentials, uses a separate build directory and displays a synthetic-data banner. No production writes or deployment.
- PASS today: independent SQL reads, actual Supabase client/PostgREST RPC calls, two authenticated sessions, partial balance (10 total, 8 collected, 2 outstanding), duplicate retry, competing collections, row-lock contention, logout/login and retrieval of a previous-run fixture. Native database restarted successfully; browser refresh/full lifecycle is NOT yet verified.
- Root cause still outstanding for customers: app/customers/page.tsx initializes SAMPLE_CUSTOMERS; app/api/leads/route.ts returns a constructed object without a database write. No customer fix was implemented in this batch. Orders/finance already use durable RPCs from batch 2.
- Local launcher excludes company seeds 002/003 and broken 004 (text driver IDs in UUID columns). Delivery migration compatibility remains a blocker; no guessed identity mapping or existing permission changes.
- Reproduction prerequisites (ignored .local-tests): pg, @embedded-postgres/windows-x64@18.4.0-beta.17 with its hydrate-symlinks script, and official PostgREST v16.3 executable at .local-tests/postgrest/postgrest.exe. Run node scripts/local_sandbox.mjs, then node scripts/test_native_persistence.mjs. Runtime credentials and database files remain ignored. Local database is betolla_isolated_20260913 on port 55439; app 3107; PostgREST 55440; adapter 55441.
- Browser attempt encountered a stopped server and a browser-tool URL-policy rejection of its generated connection-error page; no visual persistence claim. Server was subsequently restarted and API tests passed.
- git diff --check passed. Focused lint/TypeScript commands produced no completion output and were interrupted to keep this batch bounded; current-batch lint/typecheck remain unverified (earlier batch checks are not substitutes). Final measured usage: 32% five-hour, 42% weekly; daily unavailable. Unrelated android/app/build.gradle and lib/security.ts changes remain outside this batch's commit.
- Usage has five-hour/weekly windows, no daily counter. One bounded batch then stop as requested. Next exact task: implement additive customer create/edit/read persistence and connect customer UI, then verify save -> independent SQL -> Refresh -> relogin/restart/second session before marking it complete.

## Confirmed baseline
- App root is this directory. Existing Next 16.3.4 / React 19 / Supabase and Android WebView retained. Read AGENTS.md and bundled route-handler/middleware docs.
- Started from the current working tree, including eleven accounts and local Android/UI edits. Branch `fix/session-security-20260913`; initial safe-code checkpoint `801d048`. Excluded sensitive auth/security/Android build files from the initial checkpoint; retained those working files.
- Company sources are outside this repository. Inspected top-level names/sizes/types and the KPI workbook's sheet names and first rows only, without editing/importing company records. KPI sheets cover daily operations, sales, leads and cleaning.
- Android loads a configurable web URL and supports RTL; no Android rebuild or device test in this batch.

## Batch 1: session trust boundary
- `lib/auth.ts`: removed default signing secret; require an explicit JWT_SECRET of at least 32 bytes; pin HS256; require expiration/issued-at/issuer/audience; match identity, role and representative against current registered accounts; safely reject malformed cookies and non-string login inputs.
- Removed eleven inline account passwords from code. Their current values were retained in ignored `.env.local` as `BETOLLA_ACCOUNT_PASSWORD_1` through `_11`, in existing account order. This preserves local login; it is not database-backed credential storage. Confirmed all eleven environment values decode exactly, including special characters.
- Generated a random local JWT_SECRET because none was configured. Do not copy this secret into documentation, logs or Git. Existing tokens must be replaced by a fresh login after restarting the local server.
- `app/api/auth/password/route.ts`, `middleware.ts`, profile modal: require a valid session, restrict changes to that session's own username, validate input types, and attach the existing bearer token from the UI. Password update durability remains unresolved (runtime map).
- `scripts/test_session_security.mjs`: disposable random credentials, no environment loading, network or external notifications. Tests token round trip, twelve invalid tokens, malformed cookies, missing/weak keys, and five password-handler rejection cases.
- Signing keystore excluded from future tracking; its local file is retained. Older Git history still needs credential/signing-material review before any publication; history was not rewritten.

## Validation
- Isolated session/security test: PASS. Focused lint for auth, password handler, middleware and new test: PASS. Final TypeScript `--noEmit --incremental false`: PASS.
- Production build: PASS after allowing Google Fonts access (first restricted build could not fetch Cairo). No deployment or production database writes.
- Full lint: FAIL, 602 errors and 5432 warnings across the project. No broad automatic fixes. Logs are ignored local files.
- No live browser/device authentication or database persistence/concurrency tests. No company import, migrations, remote push or outgoing customer messages.

## Deployment prerequisites and next batch
- Provision JWT_SECRET and all eleven server-only account password variables through the deployment secret store before deploying. Missing settings fail closed. Local environment is not shipped in Git. Do not restore default signing keys as rollback; use a previous reviewed application version with managed secrets.
- Highest next priorities: replace runtime password overrides and finance invoice/payment arrays with durable server-side storage, canonical account IDs, password hashes and session invalidation; use transactional, idempotent collection posting and ownership checks. Finance currently serves sample invoices; leads POST returns success without saving. These are NOT production-ready features.
- Review actual database schema/RLS and identity mapping before preparing additive migrations with a rollback plan. No production migration is authorized. Keep finance/order amount versus collection/receivable definitions separate as requested.

## Batch 2 — durable orders and collections (priority changed)
- Authentication/password/security work deferred; previous fixes and unrelated local edits preserved.
- Replaced order/finance simulation routes and UI arrays with existing Supabase-table reads and atomic RPC writes; partial/full payments, exact JOD balances, safe retries, transfer-reference deduplication, and persisted status transitions. UI waits for confirmation and retains failed commands for retry; no outgoing notifications.
- Added `lib/business*.ts`, migrations 005/006, focused database/client tests. Existing rows/policies untouched; no company import, remote migration or deployment.
- PASS: actual SQL + route tests in disposable PGlite (legacy balances, rollback, lost-response retry, partial/full payments, duplicates, roles, close/reopen persistence); client retry tests; TypeScript; build. Focused lint: 0 errors, 17 existing unused-import warnings in the two UI pages; unrelated lint deferred.
- Unverified: hosted Supabase/PostgREST, real concurrent database sessions, browser/mobile UI, legacy rep UUID → account ownership mapping. Migration/configuration must be verified in staging before business usage; absent setup fails explicitly.
- Next: staging verification of migrations and ownership, then refund/stock integration. Review/rollback and isolated test instructions: `supabase/migrations/BUSINESS_PERSISTENCE.md`. No production changes authorized.
